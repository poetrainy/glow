export type indexPlansType = {
  copy: string;
  plan: string;
};

export type conditionsType = {
  title: string;
  list: string[];
};

export type optionType = {
  copy?: string;
  title: string;
  price: string;
  priceNotes?: string;
  text: string;
  textNotes?: string;
};
